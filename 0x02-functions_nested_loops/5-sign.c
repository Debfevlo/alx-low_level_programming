#include "main.h"
/**
 * print_sign - prints the signof a member
 * @c: the int to check
 * Return: 1 and prints + if n is greater than zero
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1)
	}
	else if (n == 0)
}
